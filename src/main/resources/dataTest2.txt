
SELECT I.NAME, I2.SUMMARY
FROM ISSUES I
JOIN ISSUESLISTS I2 ON I.ID = I2.ID;


SELECT U.ID, I.NAME
FROM USERS U JOIN ISSUES I ON U.ID = I.ID
GROUP BY I.ID


SELECT U.FIRSTNAME, U.LASTNAME, COUNT(*) AS TASK_NUMBER
FROM USERS U JOIN ISSUES I ON U.ID = I.ID
GROUP BY I.ID
